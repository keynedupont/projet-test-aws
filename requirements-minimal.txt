# Dépendances minimales pour démarrer rapidement
# Installation: pip install -r requirements-minimal.txt

# Core ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# FastAPI (essentiel)
fastapi>=0.110
uvicorn[standard]>=0.23
pydantic>=2
pydantic-settings>=2

# Base de données
sqlalchemy>=2
alembic>=1.12
# SQLite est inclus dans Python, pas besoin de driver supplémentaire

# Authentification
passlib[argon2]
PyJWT[crypto]
email-validator

# Templates web
jinja2
python-multipart
httpx

# Rate limiting
fastapi-limiter>=0.1.5
redis>=4.5.0

# Tests
pytest>=7
pytest-asyncio>=0.21

# Frontend (développement)
# Note: Installer Node.js et npm pour Tailwind CSS
# npm install -D tailwindcss @tailwindcss/forms
# npx tailwindcss -i ./src/projet/app/static/css/main.css -o ./src/projet/app/static/css/output.css --watch
