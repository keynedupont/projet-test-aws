# Dépendances de développement
# Généré depuis pyproject.toml avec: pip install -e ".[dev]"

# Core ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Dev tools
pytest>=7
pytest-asyncio>=0.21
httpx>=0.24

# FastAPI (si activé)

fastapi>=0.110
uvicorn[standard]>=0.23
pydantic>=2
pydantic-settings>=2
sqlalchemy>=2
passlib[bcrypt]
python-jose[cryptography]
email-validator
jinja2
python-multipart
httpx



# MLflow (si activé)

mlflow>=2.12
waitress
python-dotenv
gunicorn==21.2.0
pyyaml


# Airflow (si activé)


# DVC (si activé)

